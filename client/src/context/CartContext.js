// this is a useContext  of cart in shop It contains useState() of cart , addToCard (id ) , removeFromCard = (id) , changeQuantity
import { createContext } from "react";

export default createContext(null);
